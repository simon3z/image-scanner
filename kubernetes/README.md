# Kubernetes Image Scanner Service

It is possible to run image-scanner as a Kubernetes and OpenShift application
by creating the relevant entities:

    $ kubectl create -f image-scanner-app.yaml --namepsace=default
